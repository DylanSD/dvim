import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class VarsTest {

    @BeforeEach
    void setup() {
    }

    @Test
    void getStrVar() {
        /*Vars.putStrVar("harpoon_list", "first");
        Vars.putStrVar("harpoon_list", "second");
        Vars.putStrVar("harpoon_list", "third");
        String next = Vars.getNext("harpoon_list");
        assertEquals("first", next);
         next = Vars.getNext("harpoon_list");
        assertEquals("second", next);
         next = Vars.getNext("harpoon_list");
        assertEquals("third", next);
        next = Vars.getNext("harpoon_list");
        assertEquals("first", next);*/
    }

    @Test
    void getAllVariables() {
    }

    @Test
    void getNextOrPrev() {
    }
}